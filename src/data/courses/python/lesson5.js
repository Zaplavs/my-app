export default {
  id: 5,
  title: "Ввод и вывод данных",
  contentBlocks: [
    {
      type: 'text',
      text: `
        <p>
          В любом приложении на Python вам рано или поздно понадобится взаимодействовать с пользователем — запрашивать у него данные или выводить результаты.
          Для этого в Python есть две основные функции: <code>input()</code> и <code>print()</code>.
        </p>
      `
    },
    {
      type: 'text',
      text: `
        <h3 class="font-bold mt-4 mb-2">Функция <code>print()</code></h3>
        <p>
          Функция <code>print()</code> выводит текст или значение переменной на экран.
        </p>
      `
    },
    {
      type: 'code',
      code: `print("Привет, мир!")\n\nname = "Алекс"\nprint(name)`
    },
    {
      type: 'text',
      text: `
        <p>
          Вы можете вывести несколько значений за один раз, разделив их запятыми:
        </p>
      `
    },
    {
      type: 'code',
      code: `age = 25\nprint("Возраст пользователя:", age)`
    },
    {
      type: 'text',
      text: `
        <h3 class="font-bold mt-4 mb-2">Форматированный вывод</h3>
        <p>
          Чтобы удобно вставлять переменные в строку, используйте f-строки:
        </p>
      `
    },
    {
      type: 'code',
      code: `name = "Мария"\nage = 28\nprint(f"Имя: {name}, Возраст: {age}")`
    },
    {
      type: 'text',
      text: `
        <h3 class="font-bold mt-4 mb-2">Функция <code>input()</code></h3>
        <p>
          Функция <code>input()</code> позволяет получить данные от пользователя. Она ожидает, пока пользователь введёт текст и нажмёт Enter.
        </p>
      `
    },
    {
      type: 'code',
      code: `name = input("Как вас зовут? ")\nprint(f"Привет, {name}!")`
    },
    {
      type: 'text',
      text: `
        <p>
          Обратите внимание: <code>input()</code> всегда возвращает строку (<code>str</code>). Если вы хотите получить число, нужно преобразовать его:
        </p>
      `
    },
    {
      type: 'code',
      code: `age_str = input("Сколько вам лет? ")\nage = int(age_str)\nprint(f"Через 5 лет вам будет {age + 5} лет.")`
    },
    {
      type: 'text',
      text: `
        <h3 class="font-bold mt-4 mb-2">Обработка ошибок (на базовом уровне)</h3>
        <p>
          Если пользователь введёт нечисловое значение, когда вы ожидаете число, программа вызовет ошибку. 
          Пока мы просто покажем это, а позже научимся обрабатывать такие ситуации правильно.
        </p>
      `
    },
    {
      type: 'code',
      code: `# Опасный код (может вызвать ошибку):\nnum = int(input("Введите число: "))\nprint(f"Вы ввели: {num}")`
    },
    {
      type: 'text',
      text: `
        <h3 class="font-bold mt-4 mb-2">Пример взаимодействия с пользователем</h3>
        <p>
          Вот пример программы, которая спрашивает имя и возраст, а затем выводит краткую информацию о пользователе:
        </p>
      `
    },
    {
      type: 'code',
      code: `name = input("Введите ваше имя: ")\nage = int(input("Введите ваш возраст: "))\n\nprint("\\n--- Результат ---")\nprint(f"Имя: {name}")\nprint(f"Возраст: {age}")\nprint(f"Через год вам исполнится {age + 1}")`
    }
  ],
  task: "Напишите программу, которая запрашивает у пользователя длину и ширину прямоугольника, а затем выводит его площадь.",
  exampleAnswer: "length = float(input(\"Введите длину прямоугольника: \"))\nwidth = float(input(\"Введите ширину прямоугольника: \"))\narea = length * width\nprint(f\"Площадь прямоугольника равна: {area}\")"
};